
var calculatePoint = (w, d, l) => {
    // const totalWins = wins * 3;
    // const totalDraws = draws * 1;
    // const totalLoses = losses * 0;

    // const totalPoints = totalWins + totalDraws + totalLoses;
    // console.log(totalPoints)

    console.log( (w * 3) + (d * 1));
}

calculatePoints(3, 4, 2)
calculatePoints(5, 0, 2)
calculatePoints(0, 0, 1)